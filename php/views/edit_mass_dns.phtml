<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <title>Edição Completa de Registros DNS em Massa</title>
    <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
    <h1>Editar Registros DNS em Massa</h1>
    <form method="post" action="../scripts/massEditHandler.php">
        <table>
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Tipo</th>
                    <th>Conteúdo</th>
                    <th>TTL</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($dnsRecords as $record): ?>
                    <tr>
                        <td><input type="text" name="name[]" value="<?= htmlspecialchars($record['name']) ?>"></td>
                        <td><input type="text" name="type[]" value="<?= htmlspecialchars($record['type']) ?>"></td>
                        <td><input type="text" name="content[]" value="<?= htmlspecialchars($record['content']) ?>"></td>
                        <td><input type="text" name="ttl[]" value="<?= htmlspecialchars($record['ttl']) ?>"></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <button type="submit">Salvar Alterações</button>
    </form>
</body>
</html>
